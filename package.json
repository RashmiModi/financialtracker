{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend/calc_expense_tracker\"",
  "build": "npm run build --prefix frontend/calc_expense_tracker",
    "server": "nodemon backend/server.js",
    "start": "node backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "react-scripts": "^5.0.1",
    "webpack": "^5.99.9",
    "webpack-cli": "^6.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
